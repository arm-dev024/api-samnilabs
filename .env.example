# Google OAuth — obtain from https://console.cloud.google.com/apis/credentials
GOOGLE__CLIENT_ID=your-client-id-here
GOOGLE__CLIENT_SECRET=your-client-secret-here

# JWT — used to sign access tokens stored in HTTP-only cookies
JWT__SECRET_KEY=your-super-secret-key-change-in-production
JWT__ALGORITHM=HS256
JWT__ACCESS_TOKEN_EXPIRE_MINUTES=30

# App
FRONTEND_URL=http://localhost:5173
APP_ENV=development
BASE_URL=http://localhost:8000

# DynamoDB — use "local" credentials for local dev, real AWS creds in production
DB__ENDPOINT_URL=http://localhost:8020
DB__REGION=us-east-1
DB__TABLE_NAME=samnilabs_users
DB__AWS_ACCESS_KEY_ID=local
DB__AWS_SECRET_ACCESS_KEY=local

# Deepgram (STT/TTS)
DEEPGRAM_API_KEY=
DEEPGRAM_VOICE_ID=aura-2-thalia-en


# OpenAI (STT/LLM/TTS/Realtime)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini